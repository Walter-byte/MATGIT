A = magic(5)
B = A(4,3)
C = A(9)
% to refer to a particular element in an array
% Using a single subscript to refer to a particular element in an array is called linear indexing
A(4,6) = 19
% for newcomers
F = A(1:4,5)
% To refer to multiple elements of an array
% row (start:end),col (start:end)
G = A(3,2:5)
H = 0:5:22
% create an equally spaced vector of values
% use WHOSE for loading parameters.
